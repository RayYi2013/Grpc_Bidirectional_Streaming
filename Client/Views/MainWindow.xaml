<ui:FluentWindow x:Class="Client.Views.MainWindow"
                 MouseDown="Window_MouseDown"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:vm="clr-namespace:Client.ViewModels"
                 Title="Client"
                 Width="600"
                 Height="700"
                 WindowStartupLocation="CenterScreen"
                 ExtendsContentIntoTitleBar="true"
                 WindowStyle="SingleBorderWindow"
                 ResizeMode="CanResize"
                 WindowBackdropType="None">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

<ui:TitleBar x:Name="AppTitleBar"
                 Title="gRPC Server for Bidirectional Streaming"
                 Grid.Row="0"
                 Margin="0,0,0,16"/>

        <!-- Connection Section -->
        <ui:Card Grid.Row="1" Margin="0,0,0,15" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" 
                           Text="Server Address" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,8"/>

                <ui:TextBox Grid.Row="1" Grid.Column="0"
                            Text="{Binding ServerAddress, UpdateSourceTrigger=PropertyChanged}"
                            PlaceholderText="http://localhost:5001"
                            Margin="0,0,10,0"
                            IsEnabled="{Binding IsDisconnected}"/>

                <ui:Button Grid.Row="1" Grid.Column="1"
                           Content="{Binding ConnectButtonText}"
                           Command="{Binding ToggleConnectionCommand}"
                           Appearance="Primary"
                           MinWidth="120"/>
            </Grid>
        </ui:Card>

        <!-- Status Section -->
        <ui:Card Grid.Row="2" Margin="0,0,0,15" Padding="15">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="{Binding StatusIcon}" 
                               FontSize="20" 
                               Margin="0,0,10,0"
                               Foreground="{Binding StatusColor}"/>
                <TextBlock Text="{Binding ConnectionStatus}" 
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Foreground="{Binding StatusColor}"/>
            </StackPanel>
        </ui:Card>

        <!-- Messages Section -->
        <ui:Card Grid.Row="3" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           Text="Messages" 
                           FontWeight="SemiBold" 
                           Margin="15,15,15,10"/>

                <ScrollViewer Grid.Row="1" 
                              VerticalScrollBarVisibility="Auto"
                              Padding="15,0,15,15">
                    <ItemsControl ItemsSource="{Binding Messages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10" 
                                        Margin="0,0,0,8"
                                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                                        CornerRadius="8">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" 
                                                   FontWeight="SemiBold"
                                                   Foreground="{Binding SenderColor}">
                                            <Run Text="["/><Run Text="{Binding Sender}"/><Run Text="]"/>
                                            <Run Text=" " FontWeight="Normal" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                <Run Text="{Binding FormattedTimestamp, Mode=OneWay}" 
                                                 FontSize="11" 
                                                 FontWeight="Normal"
                                                 Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        </TextBlock>

                                        <TextBlock Grid.Row="1" 
                                                   Text="{Binding Content}" 
                                                   TextWrapping="Wrap"
                                                   Margin="0,4,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </ui:Card>

        <!-- Input Section -->
        <ui:Card Grid.Row="4" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ui:TextBox Grid.Column="0"
                            Text="{Binding CurrentMessage, UpdateSourceTrigger=PropertyChanged}"
                            PlaceholderText="Type your message..."
                            Margin="0,0,10,0"
                            IsEnabled="{Binding IsConnected}">
                    <ui:TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding SendMessageCommand}"/>
                    </ui:TextBox.InputBindings>
                </ui:TextBox>

                <ui:Button Grid.Column="1"
                           Content="Send"
                           Command="{Binding SendMessageCommand}"
                           Appearance="Primary"
                           Icon="{ui:SymbolIcon Send24}"
                           MinWidth="100"/>
            </Grid>
        </ui:Card>
    </Grid>
</ui:FluentWindow>
